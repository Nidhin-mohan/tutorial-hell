# Mission: Data Structures & Algorithms for Coding Interviews

> Reconciled 2026-07-19 with the master career workspace at `~/Desktop/NIdhin/Learn/prep/.prep/GOALS.md` — that file is the compass for target comp/timeline; this file should be kept in sync with it, not drift independently.

## Why
Land a role at a mid-size company or startup (target **₹10-15 LPA**, the research-backed figure from `Learn/prep`'s skill-gap assessment — not the earlier ₹15-20 LPA guess this file used to carry) within the next **3-6 months** for the job search overall. DSA specifically has a tighter, harder cap: **2-3 months from 2026-07-13** (per `Learn/prep/.prep/GOALS.md`, set 2026-07-19), then a forced pivot to system design regardless of how much of this curriculum is done. The user is a strong JavaScript developer (3 years) but has essentially **no DSA background** — interviews are the one gate standing between current skill and the next job, and DSA is the part they can't yet pass.

## Success looks like
- Confidently solve LeetCode **easy** and most **medium** problems in JavaScript within ~30 minutes.
- Recognise the common interview *pattern* a problem belongs to (hashing, two pointers, sliding window, BFS/DFS, etc.) and reach for it quickly.
- State the time and space complexity of a solution out loud and justify it.
- Talk through an approach clearly while coding — interview communication, not just a silent correct answer.

## Constraints
- **Timeline: hard cap at 2-3 months** (set 2026-07-19, overrides the earlier "3-6 months" estimate). The curriculum in `index.html` is now 16 phases / ~116 items — at the 3-sessions/week floor that's more than the cap allows if every phase must close (every problem ✅) before moving on. **Resolve this in favor of the calendar, not the checklist:** when the cap hits, whatever's done is done, pivot to system design. Don't let "but the phase isn't closed yet" extend past the cap.
- **The floor is 3 sessions/week, not a daily grind.** Consistency over intensity — see the rules section in `index.html`. Never propose a plan that requires doubling up to catch up.
- **Session order is attempt-first, not lecture-first** (changed 2026-07-19, see `learning-records/`). Try the problem solo, timeboxed, before watching any lesson or reading the pattern note — passive video-first was causing boredom and disengagement. See `index.html`'s rules section for the updated session structure.
- **Starting point:** Knows JavaScript well; knows almost nothing about DSA. Zero problems solved as of 2026-07-19 despite curriculum/tooling existing since 2026-07-13-15 — self-identified avoidance pattern (plan-building instead of practicing). Lessons must build from true fundamentals and lean on existing JS fluency.
- **Difficulty bar:** Mid-size / startup — LeetCode easy-to-medium. No need for hard DP or competitive-programming tricks (yet).
- **Owns the Namaste DSA course** and rates its teaching well; finds the raw 280+-question firehose overwhelming without curation. `index.html`'s PHASES now mirror Namaste's own module order 1:1 (scoped to easy-medium), so there is exactly one curriculum, not a separately-invented one.

## Out of scope (for now)
- LeetCode hard problems and contest-style tricks.
- Advanced DP, segment trees, union-find, advanced graph algorithms.
- The FANG-only portion of the Namaste DSA course.
- Language-specific trivia outside JavaScript.
