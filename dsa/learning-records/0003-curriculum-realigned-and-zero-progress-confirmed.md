# Curriculum realigned to Namaste DSA's real order; zero problems solved so far

Status: active — supersedes the implicit assumption behind learning-records 0001/0002 that Phase 1-2 ("Arrays + Big-O", "HashMap pattern") were underway.

This workspace's own 9-phase curriculum had drifted into a second plan running alongside the actual Namaste DSA course Nidhin owns and rates highly — different problems, different order. Discovered during a `Learn/prep` check-in (2026-07-19): Nidhin confirmed **zero problems have been solved**, despite lessons 0001/0002 and two phases' worth of tracker content existing since 2026-07-13/15. The self-identified cause is an avoidance pattern — building plans/tooling instead of practicing (see `Learn/prep/.prep/assessments/0002-dsa-avoidance-pattern.md`).

**Implications for future sessions:**
- `index.html`'s PHASES now mirror Namaste DSA's actual 16-module order (scoped to easy-medium breadth) — treat this as the one curriculum, don't reintroduce a competing sequence.
- Session order flips to attempt-first: don't open a lesson/video before the learner has made a timeboxed solo attempt at the linked problem. This was previously lecture-first and contributed to boredom/disengagement.
- Don't credit "a lesson/reference card exists for this phase" as progress. Progress is a problem marked ✅ in the tracker after a real solo attempt. If a future session shows two phases "complete" again with no attempt evidence, treat that as the same avoidance pattern recurring, not genuine advancement.
